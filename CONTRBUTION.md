# 贡献 VoceSpace

状态：重构期贡献规范初稿，更新于 2026-09-15。P8 将依据最终工具链定稿。当前有效开发命令与未来计划的检查分开说明。

重构路线见 [plan.md](./plan.md)，任务和验收见 [checklist.md](./checklist.md)，依赖与组件设计见 [rebuild.md](./rebuild.md)。本文件是贡献规范正文，标准命名 [CONTRIBUTING.md](./CONTRIBUTING.md) 链接到此处。

## 1. 开发准备

1. 阅读 README、对应任务与所在目录的约定。
2. 使用仓库 pnpm-lock.yaml，避免引入其他包管理器的 lockfile。
3. 复制 .env.example 到本地配置文件，按 README 准备 vocespace.conf.json；凭据只写本地，不提交。
4. 使用隔离的 LiveKit、Redis、对象存储等测试资源。基础会议不要求所有可选服务都配置。
5. 记录 Node 与 pnpm 版本。当前 package.json 声明 Node >=18；P0 已用 .nvmrc 固定开发 Node 24.12.0、packageManager 固定 pnpm 10.27.0，并验证 Node 18/24 的本地测试；不能仅凭 engines 范围推断所有版本和部署能力已通过测试。

当前已有命令：

~~~bash
pnpm install --frozen-lockfile
pnpm dev
pnpm typecheck
pnpm test
pnpm test:watch
pnpm baseline:inventory
pnpm check:boundaries
pnpm baseline:build
pnpm lint
pnpm build
pnpm start
~~~

pnpm dev/start 运行自定义 server.js，提供 Next.js 与 Socket.IO。不要用 next start 或生成的 standalone server 替代并假定实时功能相同。pnpm start 用于生产构建后的运行。

typecheck、test、test:watch 和 baseline 命令已在 P0 接入。P1 在 Node 24 下通过 46 项测试、类型、依赖边界和隔离构建；pnpm lint 已修复工具兼容性，当前 0 错误、111 个历史警告。baseline:build 包含 features，在不含私有配置的源码副本构建，不改变开发 .next。公共接口和兼容入口见 [lib 说明](./lib/README.md) 与 [P1 报告](./docs/p1/README.md)。Playwright 和 CI 仍待后续阶段接入，不要求不存在的命令。

## 2. 选择任务与工作区

- 选择 checklist 中一个明确任务，在 PR 中引用 P 编号。
- 标明目标行为、预计范围和验收方式；发现范围变化时同步文档和工时。
- Dashboard 不进行结构重构，只有共享接口迁移必需的兼容改动属于当前范围。
- 尊重已有未提交修改，不重置、不覆盖、不自动恢复他人删除的文件。
- 缺陷修复、依赖升级和纯结构迁移尽量分开，保持可审查与可回退。

## 3. 分支、提交与 PR

分支建议使用 refactor/<任务>-<主题>、fix/<主题>、docs/<主题>、feat/<主题>。基于仓库维护者指定目标分支创建，不假定生产分支或自动合并目标。

提交采用 Conventional Commits 风格，例如 refactor(room): isolate session lifecycle。标题说明具体变化；一次提交保持逻辑完整，不夹带大范围格式化或无关生成文件。

PR 正文包含：

- 关联任务与当前问题。
- 修改后的可观察行为及接口变化。
- 已执行验证、环境、结果与证据。
- 兼容性、迁移和回退说明。
- 尚未执行的检查及原因。
- 文档与实际工时更新。

涉及 UI 时提供对应页面对比；涉及音视频时提供双端操作与实际媒体结果。不要以网页能打开代替通话验收。

## 4. 架构与代码约定

- app 负责路由和组合；features 负责业务；lib 负责通用能力；server 负责服务端支持。
- 新代码遵守 rebuild.md 依赖表；迁移期旧入口必须登记，禁止新增反向依赖。
- 公共组件使用 props/回调，不能隐式读产品 store、发业务请求或创建 Socket。
- 通用 hooks 不携带业务权限、账号、许可证或具体接口；业务 hooks 留在 feature。
- 纯工具不依赖 DOM、React、网络和业务状态；类型专用导入使用 type import。
- 客户端和服务端配置、类型与导出分离，服务端凭据不通过客户端接口返回。
- 保持现有命名和格式工具约定，避免无关代码格式化；新增模块使用一致、可描述职责的名字。
- 不为满足行数限制机械拆分；单个模块包含多个责任时拆解。

## 5. 实时订阅与媒体

- 禁止导入模块即自动建立 Socket 或媒体连接。
- 订阅和清理成对，解绑明确的 event + handler，不清空其他消费者监听。
- 明确 Room、Socket、输入轨道、派生轨道、动画、定时器及异步任务所有权。
- 离会、切房间、关闭面板和卸载时释放自身资源，不能停止其他模块拥有的轨道。
- 重连不重复执行业务写入；过期异步结果不更新新会话。
- 变更需覆盖挂载、卸载、重连、离会重入和失败恢复。

## 6. 文案、样式与接口

- 用户可见文案接入现有 i18n，并同步中文、英文和俄文；不硬编码仅一种语言的错误提示。
- 保持主要布局和主题。公共样式随组件，业务样式随 feature；谨慎修改全局样式。
- 保留语义标签、键盘操作、焦点与必要的可访问性属性。
- 默认保持 URL、平台参数、认证、API、Socket 负载和用户偏好格式。
- 确需接口变化时说明前后行为、消费者、迁移及回退。移除凭据返回时同步调用方，不保留泄露兼容路径。

## 7. 埋点、部署与 Skill

- 埋点先更新 plan.md 事件字典，再实现业务调用和验证；通过统一适配接口，不在公共 UI 内埋产品事件。
- 只上报受控属性，过滤 URL、标题和 referrer；不发送用户输入、房间名、令牌或其他禁止字段。
- 成功事件来自实际完成状态；测试禁用、脚本阻断、重复渲染和重连。
- Umami 实例未就绪时，真实接收验收保持未完成。
- 部署改动同步镜像、Compose、配置说明、安装文档与 Skill 引用。
- 保留自定义服务启动方式，验证 Socket.IO 和媒体；分别记录架构和录制支持。
- 不向镜像或构建后的客户端 JS 写入密钥；日志和健康检查只暴露必要状态。
- 安装脚本复用部署模板，已有配置先备份；重复执行不重置密钥或删除数据卷，不发送安装遥测。
- Skill 主文件与参考分层，执行新增脚本的行为验证，不只检查文本格式。

## 8. 验证要求

按变更风险执行相关检查，并记录实际结果：

| 变更 | 验证 |
|---|---|
| 文档 | 相对链接、任务编号、工时、当前/目标状态一致性 |
| 纯函数 / hooks | 有意义的行为测试、取消及清理 |
| 组件迁移 | 类型、lint、相关行为、界面回归 |
| 会话 / 设备 / 协议 | 双端通话、重连、退出重入、权限失败 |
| 服务端 / 配置 | 客户端边界、旧配置兼容、错误处理 |
| 埋点 | 事件语义、去重、过滤、禁用、真实接收 |
| Docker / Skill | 配置校验、安装、跨网络媒体、持久化、重复运行与恢复 |

测试和日志区分通过、失败、未执行、阻塞。既有失败和新增错误分别登记；不为通过检查而静默禁用规则。不要提交真实密钥、访问令牌或含用户内容的测试日志。

## 9. 缺陷报告与提交前清单

缺陷报告包含应用版本、部署方式、浏览器/系统、复现步骤、预期和实际结果，以及脱敏日志。实时问题补充连接方式、是否可双端复现和触发时机；不要附真实房间口令或账号凭据。

- [ ] PR 引用了任务编号，说明具体行为和范围。
- [ ] 没有夹带他人未提交修改或无关格式化。
- [ ] 依赖边界、公开接口、资源清理已检查。
- [ ] 相关测试已执行，未执行项说明原因。
- [ ] UI、语言、接口与旧数据兼容性已检查。
- [ ] 配置、日志、埋点和构建产物没有新增凭据暴露。
- [ ] 涉及部署或 Skill 的文档已同步。
- [ ] checklist 状态、证据、实际工时及迁移记录已更新。

合并和发布由维护者按仓库权限流程进行。本文不要求贡献者替维护者部署生产环境，也不以文档更新代替发布验收。
