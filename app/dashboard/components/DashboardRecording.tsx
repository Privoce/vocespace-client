'use client';

import React from 'react';
import { RecordingContent } from '@/app/recording/content';

export const DashboardRecording: React.FC = () => {
  return <RecordingContent showContainer={false} />;
};
