# VoceSpace 分阶段重构计划

更新日期：2026-09-15。基于当前 VoceSpace 0.5.5 代码制定。

本文是实施路线图。P0 已于 2026-09-15 完成，见 [P0 完成报告](./docs/p0/README.md)；P1 已完成，见 [P1 完成报告](./docs/p1/README.md)，P2–P8 尚未实施。执行状态以 [checklist.md](./checklist.md) 为准，架构与组件迁移细节见 [rebuild.md](./rebuild.md)，贡献约定见 [CONTRBUTION.md](./CONTRBUTION.md)。

## 1. 目标与范围

- 封装仓库内模块化 lib：组件、纯函数、浏览器能力、通用 hooks 和 LiveKit 适配；本轮不建设 workspace 或发布 npm 包。
- 拆解入会、房间、控制栏、成员、聊天、频道、设置、虚拟形象、AI、应用工具、空间创建与录制页面。
- Dashboard 不进行结构重构，仅修改共享接口迁移必需的导入、类型和调用，进行兼容回归。
- Umami 由项目负责人搭建，本项目负责事件方案、代码接入和验证。
- 安装支持已有 LiveKit 和完整自建两种方式；自建录制可选，连接用户提供的 S3 兼容存储。
- 提供安装 AI Skill、贡献规范及验收记录。

本轮不包含 UI 重新设计、LiveKit SDK 大版本升级、通信协议重设计、Kubernetes、多机高可用、独立 BI 平台或 Umami 运维。生产部署首版以 Linux 单机为目标。

## 2. 当前代码基线

技术栈：Next.js 14、React 18、LiveKit、Socket.IO、Zustand、Redis，以及自定义 Node 服务。

| 当前问题 | 重构目标 |
|---|---|
| lib 混合通用能力、业务状态、网络调用和服务端代码 | 明确通用层、业务层、客户端与服务端边界 |
| Socket 在房间页面模块中创建，多个组件反向引用页面 | 独立管理连接生命周期，移除对页面模块的依赖 |
| 设备 UI 位于 app/api/devices | API 路由与客户端 UI 分离 |
| 大组件混合布局、权限、请求、订阅和媒体管理 | 拆为容器、视图、业务 hooks 与服务 |
| 已有 controls hooks，但拆分不完整 | 复用已有成果继续迁移 |
| 未发现完整自动化测试配置 | 建立核心行为测试和双端通话验收 |
| Docker 配置与当前应用启动结构不一致 | 验证真实实时服务，而非只验证首页 |

| 重点模块 | 当前路径 | 约行数 |
|---|---|---:|
| 房间入口 | app/[spaceName]/PageClientImpl.tsx | 682 |
| 视频容器 | app/pages/controls/video_container.tsx | 1513 |
| 频道 | app/pages/controls/channel.tsx | 1449 |
| 控制栏 | app/pages/controls/bar.tsx | 843 |
| 成员 Tile / Menu / Player | app/pages/participant/ | 1155 / 1098 / 1073 |
| 聊天 / 入会预览 | app/pages/chat/chat.tsx、app/pages/pre_join/pre_join.tsx | 694 / 715 |
| 成员特效 | app/pages/participant/effect.tsx | 905 |

行数来自规划时的静态检查，用于识别风险，不作为机械拆分标准。

规划时还观察到：Dockerfile 引用 next.config.js，而仓库配置为 next.config.cjs；镜像捆绑 ARM64 LiveKit 二进制；入口脚本改写构建产物并输出环境配置；服务端存在 Redis 布尔值与字符串混用；录制配置路径涉及服务端凭据。这些列入 P6 的配置与部署验收，不视为已修复。

P0 复核结果：安装与类型检查通过，19 个行为测试在 Node 18/24 通过；隔离构建生成产物但保留原配置与 Redis 告警，独立 lint 因 Next 14/ESLint 9 不兼容失败。已固定开发 Node 24.12.0 和 pnpm 10.27.0；真实通话未执行。完整证据及已知问题见 P0 报告。

P1 复核结果：完成 lib/features/server 分层，迁移设备 UI 和业务调用；Node 24 下 46 项测试、类型、依赖扫描和生产构建通过，lint 为 0 错误/111 个历史警告。Socket 生命周期与其余业务组件仍按 P2–P4 推进；真实通话未执行。预算 16 day 保留，实际人工 day 未记录。

原有未提交删除 avo.opt.md、wx_after_test.md 不属于本次文档交付，不恢复、不纳入重构改动。

## 3. 工时口径与排期

~~~text
1 day = 2 小时
1 week = 4 day = 8 小时
阶段周数 = 阶段 day ÷ 4
~~~

按一名开发者顺序排期，不计算并行提效。工时包含实现、相关测试、评审修改和文档维护。day 表示两小时工作单元，不是八小时工作日或自然日。

| 阶段 | 工作内容 | day | 小时 | 周数 | 累计工作日 |
|---|---|---:|---:|---:|---|
| P0 | 基线、范围、测试入口与规范初稿 | 8 | 16 | 2 | D1–D8 |
| P1 | lib 分层与公共接口 | 16 | 32 | 4 | D9–D24 |
| P2 | 房间、Socket、媒体生命周期 | 12 | 24 | 3 | D25–D36 |
| P3 | 会议核心组件拆解 | 28 | 56 | 7 | D37–D64 |
| P4 | 虚拟形象、AI、应用工具等拆解 | 20 | 40 | 5 | D65–D84 |
| P5 | Umami 方案与接入 | 12 | 24 | 3 | D85–D96 |
| P6 | Docker Compose 与两种安装方式 | 18 | 36 | 4.5 | D97–D114 |
| P7 | 安装 AI Skill 与辅助脚本 | 6 | 12 | 1.5 | D115–D120 |
| P8 | 贡献规范定稿、整体验收与发布准备 | 8 | 16 | 2 | D121–D128 |
| **基础合计** | | **128** | **256** | **32** | |
| 风险缓冲 | 兼容性、设备、网络、返工 | 24 | 48 | 6 | D129–D152 |
| **含缓冲合计** | | **152** | **304** | **38** | |

缓冲约为基础工时的 18.75%，按实际问题使用。等待 Umami、域名、服务器或第三方账号的时间不计开发工时，但可能推迟日历交付。不假定具体开始日期或一周内哪四天工作。

P0 完成后依据构建基线修订估算；每阶段完成后更新剩余工时，保留原估算、调整值和原因。实际工时按投入时间记录，不用预计 day 自动填充。

## 4. 阶段依赖、交付物与里程碑

| 阶段 | 前置条件 | 主要交付物 | 完成条件 |
|---|---|---|---|
| P0 | 无 | 模块清单、行为基线、检查命令、规范初稿 | 现有问题与新增问题能够区分 |
| P1 | P0 | lib 目录、公共入口、迁移对照表 | 通用层不依赖业务页面，服务端代码不进入客户端公共入口 |
| P2 | P1 | 会话 Provider、连接和订阅 hooks | 重连、离会、再次入会不产生重复连接或监听 |
| P3 | P2 | 会议核心业务模块 | 入会、通话、成员、聊天、频道行为通过回归 |
| P4 | P3 | 非 Dashboard 的其余模块 | 媒体资源正确释放，扩展功能保持兼容 |
| P5 | P2–P4 的稳定事件入口 | 事件字典、埋点适配层、指标说明 | 触发、去重、脱敏和禁用行为可验证 |
| P6 | P1 配置边界及稳定应用 | 镜像、Compose、安装与恢复说明 | 两种安装方式均通过真实双端通话 |
| P7 | P6 | 安装 Skill、预检和诊断脚本 | 能完成安装和常见故障定位 |
| P8 | P0–P7 | 贡献规范、验收记录、迁移与回退说明 | 所有必选项有验收证据，未完成项明确列出 |

- M1（D24）：lib 边界与公共入口可用。
- M2（D64）：会议核心链路迁移完成。
- M3（D84）：Dashboard 以外的业务模块完成责任拆解。
- M4（D96）：埋点适配与业务接入完成；真实 Umami 验收需实例就绪。
- M5（D120）：两种安装方式与安装 Skill 可验收。
- M6（D128）：规范、升级恢复和整体验收完成。

P0 提前登记埋点语义和部署约束，实际实施归属 P5、P6。一个阶段只有满足完成条件并附证据后才标记完成。

## 5. Umami 埋点方案

### 5.1 目标与数据边界

回答入会放弃点、失败原因、连接耗时、重连恢复、功能使用和版本退化问题。Umami 用于产品行为分析；媒体质量诊断、错误堆栈和完整运行日志保留在日志体系。

首版不收集聊天正文、文件名、录制地址、AI 输入输出、用户名、账号 ID、房间名、访问令牌、设备 ID 或密钥。不额外建立持久用户标识，不调用 identify 绑定业务身份。

公共属性仅包含 app_version、event_schema_version、environment、deployment_mode、route_name。属性采用低基数枚举，时间使用毫秒数；不传原始异常文本、用户输入或任意对象。

### 5.2 事件字典

以下 A01–A22 均纳入 P5 首版。事件字典版本为 1；实际引入的应用版本在接入 PR 中填写，不将当前 0.5.5 视为已接入版本。去重键仅用于客户端内存，不上传房间或账号标识。

| 编号 | 事件 | 业务问题 / 触发入口 | 主要属性 | 去重单位 |
|---|---|---|---|---|
| A01 | prejoin_viewed | 入会预览实际展示，观察入口流失 | entry_source | 一次预览展示周期 |
| A02 | join_started | 用户提交或平台直入真正开始 | entry_source | 一次入会尝试 |
| A03 | join_succeeded | LiveKit 首次进入已连接状态 | duration_ms | 一次入会尝试的终态 |
| A04 | join_failed | 入会出现终止性失败 | step、error_code | 一次入会尝试的终态 |
| A05 | device_permission_failed | 设备权限请求失败 | device_kind、error_code | 一次权限请求 |
| A06 | reconnect_started | 当前会话进入重连 | transport | 每种 transport 的一次重连周期 |
| A07 | reconnect_succeeded | 当前重连恢复连接 | transport、duration_ms | 一次重连周期的终态 |
| A08 | reconnect_failed | 当前重连终止失败 | transport、error_code | 一次重连周期的终态 |
| A09 | room_left | 主动离会或最终断开被观察到 | reason、duration_ms | 一次已加入会话 |
| A10 | media_state_changed | 本地音视频状态确认改变 | kind、enabled | 一次实际状态变化 |
| A11 | screen_share_started | 共享轨道成功发布 | 无额外必填属性 | 一次共享启动 |
| A12 | screen_share_failed | 共享启动失败 | error_code | 一次共享启动失败 |
| A13 | chat_message_submitted | 消息通过校验并提交发送 | message_type | 一次发送操作 |
| A14 | channel_switch_succeeded | 频道切换完成 | 无额外必填属性 | 一次频道切换 |
| A15 | recording_requested | 开始或停止请求被服务端接受 | action | 一次录制操作 |
| A16 | recording_state_changed | 观察到录制实际状态变化 | state | 一次实际状态变化 |
| A17 | recording_failed | 请求或录制执行失败 | step、error_code | 一次失败操作或状态变化 |
| A18 | ai_analysis_started | 分析任务被接受 | feature | 一次分析任务 |
| A19 | ai_analysis_completed | 分析结果可用 | feature、duration_ms | 一次分析任务的终态 |
| A20 | ai_analysis_failed | 分析任务失败 | feature、error_code | 一次分析任务的终态 |
| A21 | avatar_changed | 虚拟形象模式成功切换 | mode | 一次模式变化 |
| A22 | app_opened | 应用工具实际打开 | app_type | 一次打开动作 |

属性约定：entry_source 区分页面、邀请入口和平台直入；transport 区分 livekit 与 socket；kind/device_kind 区分音频、视频及适用的输出设备；action 为 start/stop。其他功能、状态、步骤和错误码依据现有实现建立受控枚举，未映射值统一为 unknown，禁止以原始字符串兜底。P5-01 完成枚举清单后再进行 P5-03 接入。

### 5.3 实现原则

- 业务代码通过统一 trackEvent() 接口上报；Umami 访问集中在适配层。
- 事件名称及业务属性类型归业务层，lib/analytics 负责传输和生命周期，不内置产品事件。
- 公共 UI 暴露回调，由业务层决定是否统计。
- 入会尝试只能产生一个成功或失败终态；重连成功不能再次算入会成功。
- 聊天提交不表示送达。没有服务端确认时，不命名为送达成功。
- 点击按钮不等于功能启动成功；媒体发布、录制状态和 AI 结果在对应完成入口上报。
- 页面访问由一处统一发送，不与自动 pageview 重复；空间地址规范化为 /[spaceName]。
- 同时处理 URL、query、hash、title、referrer，避免默认字段泄露身份或房间信息。
- 未配置或关闭时不加载脚本、不发送数据；开发和测试默认关闭；用户退出统计后停止后续上报。
- 脚本加载失败或请求被阻断不得抛出影响业务的异常；不建立无界缓存或自动重试风暴。
- 异常关闭的离会事件只能尽力上报，不通过永久标识补齐用户轨迹。
- 安装脚本不发送遥测；安装成功以本地检查与通话验收证明。

Umami 支持自定义事件及发送前处理。P5 开始时记录实际部署版本，核对所需接口和发送前过滤能力；版本不支持必要过滤时保持关闭并报告差异。[事件接口](https://docs.umami.is/docs/tracker-functions)、[Tracker 配置](https://docs.umami.is/docs/tracker-configuration)。

### 5.4 指标口径

| 指标 | 计算口径 |
|---|---|
| 入会成功率 | 同一窗口成功入会次数 ÷ 入会开始次数 |
| 入会失败分布 | 按 step、error_code 汇总失败事件 |
| 入会耗时 | 成功入会的 duration_ms 分布 |
| 重连成功率 | 按 transport 分组，恢复次数 ÷ 重连开始次数 |
| 功能采用率 | 使用该功能且成功入会的统计会话数 ÷ 成功入会的统计会话数 |
| AI 完成率 | 完成次数 ÷ 已开始次数 |

分母为零时显示无数据。浏览器阻断、异常关闭、跨窗口开始与结束会产生缺失，指标用于趋势判断，不宣称精确覆盖全部使用。复杂会话关联和分位数若当前 Umami 不支持，提供导出计算口径，不追加 BI 平台开发。

### 5.5 验收

每个 A 编号都有操作步骤、预期事件和属性、实际载荷、Umami 接收记录。验证重复渲染、重连、失败重试、退出统计、脚本阻断和敏感 URL。未就绪的真实 Umami 接收验证保持未完成，不以 mock 替代。

## 6. Docker Compose 与安装设计

### 6.1 服务划分

| 服务 | 已有 LiveKit 模式 | 自建模式 |
|---|---|---|
| VoceSpace | 必需 | 必需 |
| Redis | 默认随应用提供 | 提供，并划分应用与 LiveKit 的使用边界 |
| HTTPS 入口 | 提供模板或接入现有反代 | 必需 |
| LiveKit Server | 外部提供 | 独立容器 |
| TURN | 外部 LiveKit 提供 | 配置 LiveKit 内置 TURN |
| Egress | 外部平台提供录制能力 | 可选启用 |
| S3 兼容存储 | 用户提供 | 用户提供 |
| Umami | 用户独立部署 | 用户独立部署 |

默认连接已有 LiveKit，自建及录制通过明确的 Compose 覆盖文件启用。基础安装不要求 AI、SMTP、Hyperbeam 或录制凭据。数据目录、端口、镜像版本和配置来源在安装说明中集中列出。

### 6.2 应用镜像和配置

- 保留自定义 Node 服务承载 Next.js 与 Socket.IO，运行仓库自定义入口。
- 使用常规 Next.js 构建产物和必要生产依赖，不直接用 standalone 的生成入口替代自定义服务。
- 修复配置文件名，移除镜像内 LiveKit 二进制与 Supervisor，应用以非 root 用户运行。
- 不在构建时写入部署密钥，不替换构建后的 JS 注入密钥，不输出完整环境或凭据。
- 客户端配置采用白名单，与管理员配置和服务端配置分离。
- 保留旧 JSON 配置读取兼容，增加明确的数据目录，持久化可修改配置、上传文件与清理任务数据。
- 环境变量覆盖部署字段；被覆盖字段在管理界面只读并解释来源，避免保存后不生效。
- 统一 Redis 开关、端口和数据库编号解析；校验失败应明确指向字段但不返回密钥。
- 固定工具链、镜像版本及 lockfile 安装；基础应用覆盖 amd64、arm64。
- 分开存活和就绪检查，返回最少状态，不返回内部配置。

Next.js standalone 不跟踪自定义服务文件，因此采用明确的自定义服务打包方案。[Next.js 自定义服务说明](https://nextjs.org/docs/app/guides/custom-server)。

### 6.3 网络设计与验收

- 首版生产目标是 Linux 单机，本机开发路径单独说明。
- 区分容器内部地址与浏览器公网地址，配置应用、LiveKit 和 TURN 域名。
- 分别验证 HTTPS/WSS、媒体 UDP、TCP 回退和 TURN。
- 自建采用 UDP mux，避免默认映射大范围 UDP 端口。
- 443 共享采用四层 SNI 分流，将 HTTPS 与 TURN/TLS 分别转发，不以 HTTP 反代处理 TURN。
- 以最终模板列出端口、协议、用途、绑定地址和防火墙检查命令；检查公网地址及证书有效性。

LiveKit 信令、ICE 和 TURN 使用不同入口，验收必须包含真实媒体。[LiveKit 端口说明](https://docs.livekit.io/transport/self-hosting/ports-firewall/)。

### 6.4 可选录制、升级与恢复

自建录制验证 Egress、Redis、LiveKit、外部对象存储完整链路，包括请求被接受、实际开始、实际结束和文件可读取。启用前检查凭据和能力；未配置时明确显示不可用。[Egress 部署说明](https://docs.livekit.io/transport/self-hosting/egress/)。

基础镜像双架构验收与 Egress 的架构兼容分别记录，不因为应用镜像可启动就宣称录制通过。

交付升级前备份、版本回退、卷保留和恢复演练。升级先备份配置与持久数据，记录旧镜像版本；迁移不兼容时按备份恢复。官方自动化路径不删除数据卷。

## 7. 安装 AI Skill

Skill 名称为 install-vocespace，随仓库分发：

~~~text
skills/install-vocespace/
  SKILL.md
  references/    已有 LiveKit、自建、录制、故障排查
  scripts/       环境预检、配置校验、诊断工具
~~~

SKILL.md 包含有效的 name、description、触发条件、输入、两种安装路径和完成条件。主文件保持简短，场景细节放 references。参考文件链接到 P6 唯一维护的配置模板，不复制另一套部署配置。

流程：识别已有安装 → 检查环境 → 收集必要配置 → 备份和生成配置 → 校验 → 启动 → 健康检查 → 双端通话 → 输出状态和后续可选能力。

已有配置先备份；重复运行不重置密钥、不覆盖用户设置。检查 Docker/Compose、端口、架构、域名、证书、磁盘和第三方依赖；检查失败报告阶段、证据和修复建议。遵循用户已授权范围，不扩大到无关系统设置或删除数据。服务启动不等于安装成功。

## 8. 贡献规范与发布验收

以 [CONTRBUTION.md](./CONTRBUTION.md) 为正文，[CONTRIBUTING.md](./CONTRIBUTING.md) 提供标准命名入口。P0 建立初稿，P8 按最终工具链定稿。

覆盖开发命令、分支提交、PR、lib 边界、组件与 hooks、Socket 和媒体清理、中英俄文案、样式、接口兼容、事件登记、部署与 Skill 同步、缺陷复现和验收证据。

发布前必须完成：类型/lint/build、核心行为、浏览器与双端媒体、两种安装、录制可选配置、Umami 真实接收、升级恢复和 Dashboard 冒烟。既有失败必须明确记录，禁止宣称未运行或被阻塞的检查通过。

## 9. 外部依赖与变更记录

| 依赖 | 提供方 | 最晚需要阶段 | 未就绪时影响 |
|---|---|---|---|
| Umami 实例、网站 ID、访问域名和版本 | 项目负责人 | P5-04 | 本地验证可继续，真实接收验收未完成 |
| Linux 测试主机、公网域名和证书条件 | 项目负责人 | P6 | 无法完成公网媒体和 TURN 验收 |
| 已有 LiveKit 测试环境 | 项目负责人 | P2 / P6 | 无法完成真实连接及外部模式验收 |
| S3 兼容存储和录制测试条件 | 项目负责人 | P6-05 | 录制 profile 验收未完成 |
| 第二客户端和目标设备 | 开发者 / 项目负责人 | P3 / P8 | 无法完成真实双端及设备回归 |

| 日期 | 变更 | 估算影响 |
|---|---|---|
| 2026-09-15 | 建立已确认的三份核心文档及贡献规范初稿；代码重构未开始 | 基础 128 day，缓冲 24 day |
| 2026-09-15 | 完成 P0 基线、测试入口、配置与协议记录；lint 已有问题列为 B01 | P0 人工预算 8 day 保留，P1–P8 剩余基础预算 120 day，缓冲 24 day 未使用 |
